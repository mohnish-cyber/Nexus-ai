# Optional. Leave empty to use the same origin (the Vite dev proxy or the
# FastAPI server that serves the built app). Only public values belong here -
# never put API keys in frontend environment variables.
VITE_API_BASE=
